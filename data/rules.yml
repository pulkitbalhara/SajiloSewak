version: "3.1"

rules:
  - rule: Apply Scheme Form
    steps:
      - intent: apply_scheme
      - action: utter_ask_scheme_type
      - intent: select_scheme
      - action: apply_scheme_form
      - active_loop: apply_scheme_form